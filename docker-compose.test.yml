services:
  postgres-test:
    image: postgres:17.6-alpine
    env_file:
      - .env.test
    ports:
      - "5433:5432"
    volumes:
      - pgdata_test:/var/lib/postgresql

volumes:
  pgdata_test:
